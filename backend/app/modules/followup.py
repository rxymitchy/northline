"""Follow-up manager placeholder for later.

After an approved email is actually sent (disabled in MVP), this module should:
- record date_contacted
- set a follow-up date
- track response / meeting / proposal / win-loss
- draft a non-spammy follow-up

MVP stores the schema fields only. Sending remains disabled until EMAIL_SENDING_ENABLED=true
and an explicit send implementation is added.
"""


def schedule_follow_up_placeholder() -> None:
    return None
